{
  "name": "@trace-os/cli",
  "version": "0.5.4",
  "description": "CLI for @trace-os/core — traceos emit / log / replay / why / audit",
  "type": "module",
  "license": "Apache-2.0",
  "bin": {
    "traceos": "./dist/index.js"
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build":     "tsc -b",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean":     "rm -rf dist"
  },
  "dependencies": {
  "@trace-os/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": ">=6.0.2"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
