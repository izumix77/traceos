{
  "name": "traceos",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "typecheck": "pnpm --filter @trace-os/core typecheck",
    "test": "pnpm --filter @trace-os/core test",
    "build": "pnpm --filter @trace-os/core build"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "rimraf": "^6.1.3",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "uuid": "^13.0.0"
  }
}
